/*
    NPM - Nodes Package Manager
     - Online repository for publishing Node.js projects
     - Command Line utility for installing and interacting with packages and modules
     - Makes it easy for developers to update, share and reuse code

     // Check for the installed version of NPM
        - npm -v

    // Check for the current version of NPM
        - npm install npm@latest -g // This updates all packages installed globally
        - npm -v // This checks out the latest version after you have updates all the global packages

          * Module/Library -Libraries for NodeJS
          * Package - One or more modules grouped/packaged together

*/
